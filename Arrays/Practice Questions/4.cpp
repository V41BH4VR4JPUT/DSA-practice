//Rotate an array to the right by k steps.
#include <bits/stdc++.h>
using namespace std;
void rotatory(int arr[], int n, int k){
    
}
int main(){
    int n ;
    cout<<"Enter the size of the array: ";
    cin>>n;
    int arr[n];
    for(int i = 0; i< n; i++){
        cin>>arr[i];
    }
    int k;
    cout<<"Enter the number of steps: ";
    cin>>k;

    rotatory(arr, n, k);
}